import type { NextConfig } from "next";
import * as dotenv from "dotenv";

dotenv.config({ path: ".env.local" });

const nextConfig: NextConfig = {
  // Add any existing config you might have here (if any)
};

export default nextConfig;